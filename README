CSCI-P423 Spring 2012 Git repository for srwaggon.

Each assignment is designated by its own branch whereas the most recently finished branch should reside in master, as it is the jumping-off point for the next assignment.

Furthermore, each pass resides in its own named file within the compiler/ directory, simply to make editing and copy-pasta'ing easier between passes.
